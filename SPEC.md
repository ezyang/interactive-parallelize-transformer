# SPEC — Explorable "How to Parallelize a Transformer for Training"

We are rebuilding the scaling-book chapter (source/training.md, MIT-licensed) as a
Bret-Victor-style explorable explanation. One reactive model runs the entire page:
every number, equation, diagram, and chart reads from a single shared state. The
reader scrubs a number anywhere and the consequences update everywhere.

**Read this whole file before writing anything.** Deviating from the names and
markup below breaks other agents' work.

## Files

- `index.html` — GENERATED by `build.sh` (shell-top.html + sections/*.html + shell-bottom.html). Never edit directly.
- `shell-top.html`, `shell-bottom.html` — page chrome, config bar, script tags (already written).
- `sections/01-intro.html` … `sections/08-problems.html` — chapter sections (HTML fragments, no <html>/<body>).
- `css/style.css` — design system (already written; use its classes, do not add inline styles except SVG attrs).
- `js/core.js` — reactive engine (already written; read it, the API is below).
- `js/diagrams.js` — widgets: layer-diagram, scheme-explorer, shard-grid.
- `js/collectives.js` — widgets: collective, pods-diagram.
- `js/charts.js` — log-log chart engine + widgets: roofline-chart, xy-explorer.
- `js/meters.js` — widgets: bound-meter, mem-meter.
- `js/pipeline.js` — widget: pipeline-schedule.
- `js/roofline.js` — widgets: overlap-timeline, roofline-curve (already written).

Everything is plain browser JS (no modules, no build step, zero network deps).
Each js file is an IIFE that reads `window.Core` / `window.Widgets`.

## Shared state (window.Core)

Variables, defaults, meaning (all plain numbers):

| key | default | meaning |
|---|---|---|
| C | 4.59e14 | bf16 FLOPs/s per chip |
| Wici | 1.8e11 | ICI bandwidth per axis, bidirectional, bytes/s |
| Wdcn | 6.25e9 | DCN egress bandwidth per chip, bytes/s |
| HBM | 96e9 | HBM bytes per chip |
| D | 8192 | d_model |
| F | 28672 | d_ff |
| L | 80 | layers |
| B | 2e6 | global batch, tokens |
| X | 512 | shards of data/FSDP parallelism |
| Y | 8 | shards of tensor parallelism |
| MX | 2 | mesh axes carrying X |
| MY | 1 | mesh axes carrying Y |
| Z | 4 | pipeline stages |
| Mmicro | 8 | microbatches |
| MFU | 0.4 | assumed model FLOPs utilization for wall-clock estimates |
| podSize | 8960 | chips per ICI domain (pod: a full TPU v5p SuperPod; the NVLink domain under GPU presets) |
| gpu | 0 | 0 = TPU-style mesh, 1 = GPU switched fabric (set by hardware presets) |
| E | 1 | MoE: routed experts per layer (1 = dense); set by model presets |
| k | 1 | MoE: experts activated per token (routed top-k; shared experts are folded into F, not k) |
| EP | 8 | expert-parallel degree (chapter-12 calls this axis Z; we rename to avoid the pipeline-stage Z) |

### HARDWARE EPIC (staged; SOURCES.md is ground truth once it exists)
① SOURCES.md: every hardware number the page uses — spec AND measured — with a
   primary-source URL, the exact quoted figure, and retrieval date. Estimated
   values are marked ESTIMATE with reasoning.
② Hardware provenance table (id="hardware-table", after the notation tables,
   mirroring the model table): one row per preset (click-to-load), columns
   C spec/measured · W_link · W_scale-out · HBM; every cell carries a data-tip
   citing its source verbatim from SOURCES.md; ≈ marks estimates. All spec
   numbers currently in presets must be reconciled against SOURCES.md.
③ Measured mode: state keys meas (0/1, machine-bar segmented control in the
   Hardware group), effC/effIci/effDcn (defaults 1, set per hardware preset from
   SOURCES.md). Derived effective values Ce/Wie/Wde = raw × (meas ? eff : 1);
   alpha and alphaDcn are redefined on effective values; the formula sweep moves
   every direct C/Wici/Wdcn use in live chrome and widgets to Ce/Wie/Wde.
   In measured mode the MFU tooltip re-describes itself as losses beyond
   sustained matmul (no silent double-counting). Chapter .eq blocks stay verbatim.
④ Hardware terminology layer: term tokens (like the axis-notation layer) render
   hardware-dependent words per the gpu flag — TPU↔GPU, pod↔node, ICI↔NVLink,
   MXU↔tensor core, SuperPod↔SU… TPU mode = the chapter's exact wording;
   GPU mode is a disclosed global transformation (howto convention line).
   Sentences that cannot survive word-swap become t-show variant pairs, with
   chapter-12 text supplying GPU variants where it has them.

### AXIS-NOTATION TOGGLE (rendering-layer only)
The chapter names its mesh axes X (data/FSDP) and Y (tensor parallelism); this
page renders them **DP** and **TP** by default, with a machine-bar toggle back
to the chapter's X/Y. Rules:
- State key `nota` (0 = DP/TP default, 1 = chapter X/Y). INTERNAL NAMES NEVER
  CHANGE: state keys stay X/Y, every data-expr stays X/Y, URL hashes stay X/Y.
- `Core.axisName("X"|"Y"|"MX"|"MY")` returns the display name for the current
  mode ("DP"/"TP"/"M_DP"/"M_TP" or "X"/"Y"/"M_X"/"M_Y"). All widget-generated
  labels/readouts/flags MUST use it (widgets re-render on toggle automatically
  since nota is state).
- In HTML: `.v.v-X`/`.v-Y` tokens and `<sub>` elements whose text is exactly
  X or Y inside `.shape`/`.eq` contexts are re-rendered by core on toggle
  (core tags them at boot). Bare X/Y mentions in chapter prose must be wrapped
  as `.v` tokens so the toggle is complete — mixed notation is a defect.
- M_X/M_Y tokens: the letter M keeps its token; its <sub>X</sub> swaps like
  any other subscript.
- Disclosure: one line in the header's convention list (global substitution,
  chapter notation one toggle away) — no per-site Δ notes.

### F-CONVENTION v2 (chapter-12 alignment — supersedes the "activated F" folding)
The ambiguity ("is F sparse or dense?") is resolved the way chapter 12 resolves it:
- **F = the per-expert feed-forward width** (config `moe_intermediate_size`; for a
  dense model this is simply d_ff, so the training chapter's text stays true as written).
- **k = experts activated per token, INCLUDING shared experts** (disclosed: equals
  chapter-12's plain top-k whenever there are no shared experts). **Activated width = k·F.**
- **E = total experts holding weights, including shared** (1 for dense). **Total width = E·F.**
- **Uniform substitution rule** for every live formula, widget, and .eq-live line
  (the chapter's displayed .eq blocks stay verbatim — they are the E=k=1 case; the
  generalized numbers live in the adaptation chrome, plus t-show E>1 notes):
  | dense chapter term | generalized |
  |---|---|
  | FLOPs / activation width F (T_math numerators, TP's sharded width, activation memory D+2F) | k·F |
  | weight bytes / weight width F (DP-AllReduce & FSDP-gather comms, param counts) | E·F |
  Consequences (all must reduce to the dense form at E=k=1):
  T_math = 4·B·D·k·F/(shards·C) fwd (8·… bwd); DP/FSDP T_comms = 4·D·E·F/(W·MX) fwd
  (8·… bwd); DP/FSDP bound B/X > (E/k)·α/MX; TP bound Y < MY·k·F/α; mixed
  T_fsdp = 4·D·E·F/(Y·W·MX), T_tp unchanged, X_opt = √(B·MX·N/(E·F·MY)), floor
  B/N > α²·E/(MX·MY·k²·F); P (param count) = 2·D·E·F·L; activation memory
  2·L·B·(D+2·k·F); pipeline hop unchanged (2·B·D/Mmicro); pipeline stage compute
  4·(B/Mmicro)·D·k·F·ceil(L/Z)/C; EP exactly chapter-12's: T_math = 4·B·k·D·F/(EP·C).
- Model-table presets store per-expert F (e.g. DeepSeek-V3 {D:7168, F:2048, L:61,
  E:257, k:9}; Kimi K3 {7168, 3072, 93, 898, 18}; GLM-5.2 {6144, 2048, 78, 257, 9};
  DSv4-Pro {7168, 3072, 61, 385, 7}; Qwen3.8 {8192, 2048, 92, 513, 11}; Inkling
  {6144, 3072, 66, 258, 8}; MiniMax-M3 {6144, 3072, 60, 129, 5}).
- **MoE data-parallelism penalty** (now exact): B/X > (E/k)·C/W_collective.
- **Expert parallelism** (shard experts EP ways, AllToAll activations to their experts and back):
  T_math = 4·B·D·F/(EP·C). GPU cross-node AllToAll (chapter 12):
  T_comms = 4·B·D·((EP−8)/EP)·min(8·k/EP, 1)/Wdcn·podSize-form — use chapter-12's exact expressions;
  guard EP ≤ 8 (in-node) as ≈free on the NVLink domain. Two viable regimes (chapter-12 takeaway):
  small EP (1–2 nodes) with small F, or F > 8·C/W_node and EP up to E.

### V3 layout contract — Chapter-12 merge (supersedes the "#gpus section" layout above)
The GPU chapter is no longer a separate appendix; it is INTERLEAVED, primarily
subtractively: where chapter 12 re-derives a bound the training chapter already
derived, the page keeps ONE derivation and carries the GPU delta in a
`t-show`-gated passage or ✦ margin note (visible under GPU presets). Chapter-12
material that is genuinely new gets its own place in the flow:
- `#expert-parallelism` (new section, sections/05b-ep.html, between #mixed and
  #pipelining): chapter-12's EP treatment, hardware-neutral framing, E/k/EP live.
- The MoE-DP penalty joins #data-parallelism as a `t-show` (E>1) passage.
- Chapter-12's pipelining reasons merge into #pipelining as gpu-conditional notes.
- `#gpus` shrinks to: the network mapping, the bandwidth table, collective costs
  + SHARP + the empirical-370GB/s caveat, and the worked GPU examples. Same id.
- The takeaways tables gain an EP row (formulas per above).
| pD | 5120 | problems: LLaMA-2 13B d_model |
| pF | 13824 | problems: d_ff |
| pL | 40 | problems: layers |
| pNH | 40 | problems: n query heads |
| pNK | 40 | problems: n KV heads |
| pH | 128 | problems: head dim |
| pV | 32000 | problems: vocab |
| pB | 16e6 | problems: batch tokens |

Derived values available in every expression (recomputed each change):
`N` (=X*Y), `alpha` (=C/Wici), `alphaDcn` (=C/Wdcn), `P` (=2*D*F*L, MLP-stack param
count), plus all of `Math` (sqrt, min, max, pow, log2, ceil, floor, round, abs, PI, exp, log10).

### Core API (js/core.js — already implemented)

- `Core.get()` → state object (do not mutate).
- `Core.set({key: value, ...})` → updates, clamps, notifies all subscribers.
- `Core.subscribe(fn)` → fn(state) on every change (and NOT at registration).
- `Core.evalExpr(str)` → compiled evaluator; `Core.evalNow(str)` → value now.
- `Core.fmt(value, fmtName)` → formatted string. fmt names:
  `int` 2,550 · `sig3` 3 sig figs w/ separators · `si` 7.6M / 2.5k / 1.2B ·
  `bytes` 42.0 TB · `e` 4.6e14 · `flops` 459 TFLOP/s · `bw` 180 GB/s ·
  `time` auto ns/µs/ms/s/min/hr/days · `days` 17.3 days · `pct` 38% · `x` 3.2× ·
  `tokens` same as si · `chips` same as int.
- `Core.onDimHover(fn)` → fn(dimOrNull) when a dimension token (B/D/F/X/Y/L/C/W/N) is
  hovered anywhere; widgets may highlight matching parts. Fire your own via
  `Core.dimHover('B')` / `Core.dimHover(null)`.
- `Widgets.register(name, factory)` where `factory(el, opts)` returns
  `{update(S)}`; core mounts every `[data-widget]` at boot, calls update(S)
  immediately and on every state change. `opts` = parsed JSON of `data-opts`.

## Declarative markup (what section HTML uses — core.js wires these automatically)

- **Scrubbable number** (green, draggable):
  `<span class="t-var" data-var="B" data-min="1e4" data-max="1e9" data-scale="log" data-fmt="si"></span>`
  Optional `data-scale="log"|"lin"`, `data-snap="pow2"|"125"|"int"`, `data-step="1"`.
  Core renders the value, drag left/right to change, double-click resets that var,
  keyboard arrows work. NEVER put text inside; core owns the content.
- **Derived number** (blue): `<span class="t-out" data-expr="8*D*F/(Wici*MX)" data-fmt="time"></span>`
- **Conditional text**: `<span class="t-if" data-expr="B/X > alpha/MX" data-then="compute-bound ✓" data-else="communication-bound ✗"></span>`
  (also gets class `t-if-true`/`t-if-false` for green/red styling).
- **Conditional block**: `<div class="t-show" data-expr="Y > MY*F/alpha">…</div>` (hidden unless truthy).
- **Preset button**: `<button class="t-preset" data-set='{"D":8192,"F":28672,"L":80}'>LLaMA-3 70B</button>`
  Rows of presets: `<div class="preset-row"><span class="preset-label">Try:</span> …buttons…</div>`
- **Widget mount**: `<div class="widget" data-widget="roofline-chart" data-opts='{}'></div>`
  Wrap wide widgets in `<figure class="breakout">…<figcaption>…</figcaption></figure>`.

### Formula markup (no LaTeX — hand-rolled reactive HTML)

- Display equation: `<div class="eq"> … </div>`; inline: `<span class="eq-i">…</span>`.
- Variable token: `<i class="v v-B">B</i>` — classes exist for v-B v-D v-F v-L v-X v-Y v-N v-C v-W v-Z v-M (W covers Wici/Wdcn, M covers MX/MY). Tokens are auto-wired for dim-hover cross-highlighting by core. Subscripts: `<i class="v v-W">W<sub>ici</sub></i>`.
- Fraction: `<span class="frac"><span>numerator</span><span>denominator</span></span>`
- Square root: `<span class="sqrt"><span>radicand</span></span>`
- Operators: use `·` for times, `→` for produces, plain `&gt;` `&lt;` `=` `≈`.
- Shape/einsum notation: `<span class="shape">In[B<sub>X</sub>, D]</span>` (monospace-ish styling).
- Live evaluation line under an equation:
  `<div class="eq-live">right now: <span class="t-out" data-expr="…" data-fmt="time"></span> vs <span class="t-out" data-expr="…" data-fmt="time"></span> → <span class="t-if" data-expr="…" data-then="compute-bound" data-else="comms-bound"></span></div>`

### Prose components

- Section: `<section id="data-parallelism"><h2>Data Parallelism</h2>…</section>` (h2 for main sections, h3 for subsections; ids in kebab-case are used by the TOC in shell-bottom — REQUIRED ids listed per section below).
- Takeaway: `<div class="takeaway"><strong>Takeaway:</strong> …</div>`
- Note box: `<div class="note"><strong>Note:</strong>…</div>`
- Sidenote (was footnote): `<span class="sn"><button class="sn-ref" aria-label="sidenote">◦</button><span class="sn-body">…</span></span>` — CSS handles margin placement / inline expansion.
- Collapsible algorithm/detail: `<details class="algo"><summary>Full algorithm: forward and backward pass</summary><div class="algorithm"><ol><li>…</li></ol></div></details>`
- Communication ops inside algorithms/prose: `<span class="op op-ag">AllGather</span>`,
  `op-rs` ReduceScatter, `op-ar` AllReduce (styled pills).
- Code: `<pre class="code"><code>…</code></pre>` (escape HTML!).
- Tables: `<table class="tbl">…`.
- "Try it" prompts (Victor-style nudges): `<p class="try">Try: drag <span class="t-var" data-var="B" …></span> down and watch the ratio flip.</p>`

## Palette & chart rules (from the dataviz design system — binding)

CSS custom props already defined in style.css:
- Surfaces/ink: `--surface:#fcfcfb --page:#f9f9f7 --ink:#0b0b0b --ink-2:#52514e --muted:#898781 --grid:#e1e0d9 --axis:#c3c2b7 --hairline:rgba(11,11,11,.10)`
- Categorical series slots IN THIS ORDER (never re-order, never cycle):
  `--s1:#2a78d6` (blue) `--s2:#eb6834` (orange) `--s3:#1baf7a` (aqua) `--s4:#eda100` (yellow) `--s5:#e87ba4` (magenta) `--s6:#008300` `--s7:#4a3aa7` `--s8:#e34948`
- Scrubbable green `--scrub:#006300`; derived blue `--derived:#1c5cab`.
- Dimension token colors: `--dim-B:#4a3aa7 --dim-D:#1c5cab --dim-F:#9a4a00 --dim-X:#00695f --dim-Y:#ad2f2f --dim-L:#52514e --dim-N:#0b0b0b --dim-C:#52514e --dim-W:#52514e --dim-Z:#00695f --dim-M:#ad2f2f`

Series → meaning assignment (consistent across ALL charts):
- FLOPs / T_math reference line: ink `#0b0b0b`, dashed 2px.
- Data-parallel / FSDP comms: `--s1` blue.
- Tensor-parallel comms: `--s2` orange.
- Mixed FSDP+TP (optimal) comms: `--s3` aqua.
- Pipeline blocks: forward `--s1`, backward-for-input `--s2`, backward-for-weights `--s3`, bubble = surface with diagonal hatch.
- Chip identity in sharding diagrams: chips are numbered and colored by slot order s1,s2,s3,s4,s5,s6,s7,s8 (max 8 distinct chips drawn; beyond that, draw "…" ellipsis chips in gray).

Chart chrome rules (binding): 2px lines round cap; hairline solid gridlines in `--grid`; axis text 11px system sans in `--muted`; NO dual axes; legend always for ≥2 series (line-key swatches); crosshair + single tooltip listing all series at the hovered x (built via textContent, never innerHTML); ≥24px hit targets; direct end-labels only where they fit; numbers on axis ticks at clean powers (log axes tick at 10^k with SI labels); text never in series colors — use ink/muted with a colored line-key beside it. All charts SVG, sized via viewBox + ResizeObserver, font system-ui.

## Physics (the model — every widget/section uses EXACTLY these, per-layer, bf16)

- alpha = C/Wici (= 2550 at defaults). alphaDcn = C/Wdcn (= 73,440).
- **DP** (backward pass): Tmath = 8·B·D·F/(X·C); Tcomms = 8·D·F/(Wici·MX). Compute-bound iff B/X > alpha/MX.
- **FSDP** (forward pass modeled): Tmath = 4·B·D·F/(X·C); Tcomms = 4·D·F/(Wici·MX). Same bound.
- **TP** (forward): Tmath = 4·B·D·F/(Y·C); Tcomms = 4·B·D/(Wici·MY). Compute-bound iff F > Y·alpha/MY, i.e. Ymax = MY·F/alpha.
- **Mixed FSDP+TP** (forward, N=X·Y): Tmath = 4·B·D·F/(N·C); Tfsdp = 4·D·F/(Y·Wici·MX); Ttp = 4·B·D/(X·Wici·MY); Tcomms = max(Tfsdp, Ttp). Xopt = sqrt(B·MX·N/(F·MY)). Compute-bound iff B/N > alpha²/(MX·MY·F).
- **DCN cross-pod DP** (backward): Tmath = 8·B·D·F/(N·C); Tcomms = 8·D·F/(podSize·Wdcn). Compute-bound iff B/pod > alphaDcn.
- **Memory** (per chip, bytes): paramsBytes = 2P, optBytes = 8P (Adam, fp32 m+v); DP: params+opt replicated (÷1), FSDP: ÷X, mixed: ÷N. Checkpointed activations (3 per layer) total = 2·L·B·(D+2F) bytes, sharded ÷X (÷(X·Y) for mixed). Max params with pure DP + Adam = HBM/10.
- **Pipeline** (GPipe-style, bwd block = 2× fwd width): bubble fraction = (Z−1)/(Mmicro+Z−1); with "1F1B" same bubble but lower activation memory; with "overlap dW/dx" (DeepSeek-like) the widget shows the bubble filled by dW work.
- **Wall-clock step time** (for examples): T = 6·B·params/(N·C·MFU) with params from context (use P for MLP-stack, or a stated real-model count).
- Roofline chart series as functions of B with all-N-chips: Tmath(B)=4·B·D·F/(N·C); FSDP-only comms = 4·D·F/(Wici·MX) const; TP-only comms = 4·B·D/(Wici·MY); mixed-optimal comms = (4·D/Wici)·sqrt(B·F/(MX·MY·N)). All use the live MX/MY so every view shares one model.

## Widget contracts (exact names/opts; implementers keep these; sections mount them)

| name | opts | what it shows |
|---|---|---|
| `layer-diagram` | `{}` | The simplified transformer layer In[B,D]→W_in[D,F]→W_out[F,D]→Out[B,D] as flow boxes; box edges labeled and proportional to log2 of live dims; hovering a dimension edge fires dimHover. |
| `scheme-explorer` | `{}` | Tab strip (DP / FSDP / TP / FSDP+TP). For the active scheme: the einsum syntax line, the four arrays drawn with shard grid coloring by chip, per-chip local shapes, and the list of comms ops incurred (fwd + bwd). The overview centerpiece. |
| `shard-grid` | `{"scheme":"dp"}` (dp/fsdp/tp/mixed) | Section figure: In and W_in (and W_out for tp/mixed) rectangles subdivided among chips (colors = slot order; grid uses min(X,4)×min(Y,2) representative chips with ellipsis affordance), a chip strip below; hover chip ⇄ highlight its shards; captions give logical + local shapes with live numbers. |
| `collective` | `{"op":"allreduce","bytesExpr":"2*D*F","axisVar":"MX","label":"AllReduce(dW_in)"}` | Ring of 8 chips animating the collective (allgather / reducescatter / allreduce / ar-decomp which shows AllReduce = AG+RS); play/pause + progress scrub; readout of bytes moved and time = (1 or 2)·bytes/(Wici·M). |
| `bound-meter` | `{"scheme":"dp"}` (dp/fsdp/tp/mixed/dcn) | Two horizontal bars (T_math vs T_comms per layer, µs) on a shared linear scale, values at bar ends, verdict pill (compute-bound/comms-bound) + headroom ×. Uses Physics formulas above. |
| `roofline-chart` | `{}` | Log-log chart, x = batch B (1e4..1e9), y = time/layer; series per Physics; draggable vertical marker at current B (drag sets B); view toggle: absolute times ↔ ratio T_math/T_comms; crosshair tooltip. |
| `xy-explorer` | `{}` | For fixed N: x-axis = X (powers of 2 from max(1,N/Fcap) … N), curves Tfsdp(X) blue, Ttp(X) orange, max() emphasized, Tmath dashed ink; draggable marker on current X (sets X, Y=N/X); Xopt annotated with dotted drop-line; compute-bound region shaded. |
| `mem-meter` | `{"scheme":"fsdp"}` | Stacked horizontal bar of per-chip HBM: params / optimizer / activations vs HBM capacity tick; overflow shown in red hatch with "doesn't fit". |
| `pipeline-schedule` | `{}` | Gantt of devices×time; mode toggle (naive / 1F1B / overlap-dW); scrubbable Z and Mmicro bound to state; bubble % + per-device utilization; legend. |
| `pods-diagram` | `{}` | K pods (K = ceil(N/podSize), draw ≤4) each a mini 3D-ish mesh, DCN links between; live check per-pod batch vs alphaDcn; readout. |
| `overlap-timeline` | `{}` | (js/roofline.js, already written) Two tracks, compute vs network, per-layer times to scale; exposed comms hatched red; toggle weights-move vs activations-move. |
| `roofline-curve` | `{}` | (js/roofline.js, already written) Log-log network roofline: utilization vs per-chip batch B/X; ridge at alpha/MX; TP flat overlay; draggable operating dot sets B. |

Widgets must: render entirely inside their `el`; rebuild cheaply on update(S) (ok to
rerender SVG wholesale, but preserve in-flight drag state); use only palette CSS vars
via `var(--s1)` etc. (getComputedStyle if needed in SVG attrs — or hardcode the hex
from this spec, they are stable); include aria-labels; never call Core.set during
update (only from user events).

## Section fragments — content map (source lines in source/training.md)

Adapt the chapter text faithfully — keep EVERY takeaway, formula, algorithm,
example, problem, and the appendix. This is a translation into an interactive
medium, not a summary. Where the chapter states a static number (2,550; 9B params;
47,000 chips; 17 days; …) make it LIVE (t-out with the formula) and where it assumes
a value (batch 40M, F=30k) make it SCRUBBABLE (t-var, with data-min/max spanning
sensible ranges and a preset button restoring the chapter's value). Convert each
"here's the full algorithm" details block. Keep the authors' voice; light editing
for the interactive framing is encouraged ("notice", "try", "drag"). Add a `try`
nudge line wherever a scrub is especially illuminating. Do NOT include scripts.

| file | source lines | required section id(s) | required widgets |
|---|---|---|---|
| 01-intro.html | 88–157 | `#scaling` (h2 "What Do We Mean By Scaling?") | layer-diagram, scheme-explorer |
| 01b-roofline.html | (new material, already written) | `#roofline` "First, Feel the Roofline" | overlap-timeline, roofline-curve — an intuition primer added by this adaptation, not from the chapter; later sections may back-reference `#roofline` ("does the comms fit under the compute?") |
| 02-dp.html | 158–243 | `#data-parallelism` | shard-grid(dp), collective(allreduce, bytesExpr "2*D*F"), bound-meter(dp), mem-meter(dp) |
| 03-fsdp.html | 244–306 | `#fsdp` | shard-grid(fsdp), collective(ar-decomp), bound-meter(fsdp), mem-meter(fsdp) |
| 04-tp.html | 307–382 | `#tensor-parallelism` | shard-grid(tp), bound-meter(tp) |
| 05-mixed.html | 383–505 | `#mixed` | shard-grid(mixed), xy-explorer, roofline-chart, bound-meter(mixed) |
| 06-pipeline.html | 506–569 | `#pipelining` | pipeline-schedule |
| 07-pods.html | 570–650 | `#pods`, `#takeaways` | pods-diagram, bound-meter(dcn); takeaways incl. the two summary tables |
| 08-problems.html | 651–732 | `#problems`, `#appendix` | mem-meter(fsdp) reuse ok; problems use pD,pF,… vars |

Notation tables from lines 96–110 go in 01-intro as `.tbl` with `v` tokens.
Sections 02–05 each end with their takeaway(s). 08-problems: each question in a
`<details class="algo">`-style reveal (`<details class="answer">`), with the given
constants scrubbable (pD, pF, …) so the ANSWER RECOMPUTES — answers must be
expressed with t-out expressions, not hardcoded numbers, wherever computable.
The LLaMA-2 param count: FFW `3*pL*pD*pF`, attn `4*pD*pNH*pH*pL`, vocab `2*pV*pD`.

Attribution (already in shell-bottom): original by Jacob Austin et al., Google
DeepMind, MIT license, link to original chapter — do not repeat per-section.

## V2 CONTRACT — restoration & disclosure (SUPERSEDES the voice rules below)

The page's philosophy changed: it is now "the original text, made interactive."
The book is MIT-licensed; its notice ships with the page (LICENSE-scaling-book.txt)
and is linked from the header and footer, so carrying the authors' text verbatim is
the *goal*. The adaptation speaks only in the margin.

Rules:
1. **Main column = the chapter's original sentences**, restored verbatim from
   source/training.md (or source/gpus.md for the GPU section). Reordering,
   summarizing, or re-voicing is a defect unless disclosed (rule 4).
2. **Global conventions** (stated in the header; NOT individually marked):
   (a) wherever the chapter printed a static number, the page renders it live
   (t-var/t-out swapped in place, sentence otherwise intact); (b) interactive
   figures + their figcaptions replace the original figures; (c) `.eq` blocks
   render the chapter's LaTeX as reactive HTML; (d) `.eq-live` annotation lines
   under equations are adaptation chrome.
3. **✦ adaptation margin notes** carry ALL adaptation voice: try-prompts,
   instructions ("drag this"), transitional sentences we added, GPU asides,
   cross-references to the primer. Markup:
   `<span class="sn adapt"><button class="sn-ref" aria-label="adaptation note">✦</button><span class="sn-body"><span class="sn-tag">adaptation</span> …</span></span>`
   anchored at the end of the relevant sentence. Convert existing `.try` blocks
   into these. An added in-flow block that cannot move to the margin (e.g. an
   added equation) keeps a ✦ note anchored to its lead-in.
4. **Δ edit notes** disclose every sentence that had to be *altered* (beyond
   rule-2 swaps) to host a live element:
   `<span class="sn edit"><button class="sn-ref" aria-label="edit note">Δ</button><span class="sn-body"><span class="sn-tag">edited</span> The chapter reads: <span class="orig">“…”</span> — reworded to host the live verdict.</span></span>`
   placed immediately after the altered passage. Quote the original faithfully.
5. Original chapter footnotes stay as plain `.sn` (◦ marker), text verbatim.
6. Never remove widgets, presets, live spans, or section ids during restoration.

## GPU support (state + mapping — grounded in source/gpus.md)

- New state key `gpu` (0/1; set by hardware presets, usable in any data-expr,
  e.g. `<span class="t-show inline" data-expr="gpu">…</span>` for GPU-only
  phrases and `data-expr="1-gpu"` for TPU-only ones).
- Hardware presets (machine bar): H100 {C:9.9e14, Wici:4.5e11, Wdcn:5e10,
  HBM:8e10, podSize:8, gpu:1, MX:1, MY:1}; B200 same but C:2.25e15, Wici:9e11,
  HBM:1.92e11; GB200 NVL72 same as B200 but podSize:72.
- The mapping that makes the whole page's math correct on GPUs (per gpus.md):
  read `Wici` as the per-GPU **egress bandwidth** into the NVLink switch fabric
  (450 GB/s H100, 900 GB/s B200); a switched fabric has no mesh axes, so
  MX = MY = 1; read "pod" as the **NVLink domain** (podSize 8 or 72); read
  `Wdcn` as the per-GPU share of node InfiniBand egress (400e9/8 = 5e10 for
  H100/B200; 3600e9/72 = 5e10 for GB200). Then alpha = C/Wici = **2,200** in-node
  (H100) and the cross-node ridge C/Wdcn = 19,800/node = **2,475**/GPU — exactly
  the chapter-12 published bounds. AllReduce = 2·bytes/W as on TPU (without
  SHARP in-network reductions, which theoretically halve it but deliver ~30%).
- Section `#gpus` ("The Same Rooflines on GPUs") lives in sections/07-pods.html
  between #pods and #takeaways, adapting gpus.md lines 599–720 under the same
  V2 rules (verbatim where used; the TPU↔GPU mapping paragraph is adaptation ✦).

## Voice & Victor principles (how to write the prose)

1. The reader is active: claims are checkable by dragging the very numbers in the
   sentence. Prefer "drag B and watch T_comms stay put" over "note that T_comms is
   independent of B".
2. One model, many views: never introduce a new toy number disconnected from state.
3. Show the thing, then the algebra: widget first or alongside, derivation in
   reach (details/aside), conclusion as a live takeaway.
4. Nothing is magic: every displayed number should be traceable — t-out tooltips
   (core adds title= with the expression) give provenance for free.
